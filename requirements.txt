# ─────────────────────────────────────────────────────────
#  Requirements – GitaVanni Vectorless RAG  (Render-ready)
#  Pipeline: BM25 + TF-IDF + RRF (NO PyTorch, NO FAISS)
#  Target: <400 MB RAM on Render free tier
# ─────────────────────────────────────────────────────────

# Web Framework
fastapi
uvicorn[standard]

# Data Validation
pydantic

# LangChain Core
langchain
langchain-core
langchain-community
langchain-text-splitters

# BM25 Sparse Retrieval
rank_bm25

# TF-IDF Sparse Retrieval
scikit-learn

# Sparse matrix serialisation (csr_matrix)
scipy

# Numerics
numpy

# LLM – Groq API (no local model needed)
langchain-groq

# PDF Loader
pypdf

# HTTP client (used internally by LangChain / Groq SDK)
httpx

# NOTE: sentence-transformers and torch are intentionally EXCLUDED.
# They add ~300 MB RAM and are not needed with BM25+TF-IDF+RRF pipeline.
